[project]
name = "virk-client"
version = "0.1.0"
description = "Python bibliotek til at tilgå virk REST services. Kræver aftale + certifikat til auth."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "pytest>=9.0.3",
    "ruff>=0.14.13",
]

